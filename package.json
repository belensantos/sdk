{
    "workspaces": [
        "./packages/**/*"
    ],
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "prettier": "3.1.1",
        "@belensantos-sdk/prettier-config": "1.0.0"
    }
}